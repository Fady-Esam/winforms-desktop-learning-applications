
using System;


namespace ShowResults
{
    class Program
    {
        static void Main()
        {
            /*
            string filePath = @"C:\Users\Fady\Downloads\نسخة البحث24.accdb";
            // Connection string for the Access database file
            string connectionString = $@"Provider=Microsoft.ACE.OLEDB.12.0;Data Source={filePath};";

            // Query to execute
            string query = "SELECT * FROM YourTableName";

            // Create a connection object
            using (OleDbConnection connection = new OleDbConnection(connectionString))
            {
                try
                {
                    // Open the connection
                    connection.Open();

                    // Create a command object
                    using (OleDbCommand command = new OleDbCommand(query, connection))
                    {
                        // Execute the query and obtain a data reader
                        using (OleDbDataReader reader = command.ExecuteReader())
                        {
                            // Read and display data
                            while (reader.Read())
                            {
                                // Example: Reading the first column from the reader
                                Console.WriteLine(reader[0].ToString());
                            }
                        }
                    }
                }
                catch (Exception ex)
                {
                
                    Console.WriteLine("An error occurred: " + ex.Message);
                }
        
            }*/
        }
    }
}











